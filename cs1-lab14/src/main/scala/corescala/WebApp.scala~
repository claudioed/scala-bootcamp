package corescala

class Request(val id: Int)

class WebApp {
  def index(request: Request): String = "id: " + request.id
  def about(request: Request): String = "about!"
  def contato(request: Request): String = "envie emails para teste@test.com"
}
